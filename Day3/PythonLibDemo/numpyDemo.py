# Numpy will help us to work on large array set with data manupulation


import numpy as np

'''dataset = np.arange(100).reshape(10,10)
print(dataset)

#write a program to find the maximum paid emplyee salary

dataset = np.arange(1000000)
max_salary = np.amax(dataset)
print(max_salary)


dataset1 = np.arange(100).reshape(10,10)
dataset2 = np.arange(100,200).reshape(10,10)
# Apply arithmetic operation after reshaping into 10*10

total_salary_two =dataset1 + dataset2
print(total_salary_two)'''

random_marks = np.random.randint(0,10,100).reshape(10,10)
print(random_marks)

